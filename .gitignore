node_modules
.next
dist
.env
.env.local
.env.*.local
.turbo

# Expo
.expo
ios/
# android/ is committed (bare workflow) so EAS doesn't regenerate it

# Supabase
supabase/.branches
supabase/.temp

# OS
.DS_Store
Thumbs.db
